[project]
name = "flyrank-scraper"
version = "0.1.0"
description = "Polite catalogue scraper for books.toscrape.com practice site"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.0",
    "beautifulsoup4>=4.12.0",
    "pydantic>=2.0.0",
]

[tool.uv]
package = false